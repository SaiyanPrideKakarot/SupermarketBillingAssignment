# SupermarketBillingAssignment

Note: To run the code simply run the code in code runner or run the script `node .\supermarketBilling.js` or `node supermarketBilling.js` where `supermarketBilling.js` is the file name of the script. The output will be returned in console.